# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DogBreeds' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DogBreeds
  pod 'Kingfisher'

  target 'DogBreedsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DogBreedsUITests' do
    # Pods for testing
  end

end
